components:
  schemas:
    APIKeyExportEntry:
      additionalProperties: true
      properties:
        created_at:
          format: date-time
          type: string
        id:
          type: string
        key_prefix:
          type: string
        last_used_at:
          format: date-time
          type: string
        name:
          type: string
        revoked_at:
          format: date-time
          type: string
      required:
        - id
        - name
        - key_prefix
        - created_at
      type: object
    APIKeyView:
      additionalProperties: true
      properties:
        agent_email:
          description: Bound inbox email for agent-scoped keys; omitted for account scope.
          type: string
        created_at:
          format: date-time
          type: string
        expires_at:
          format: date-time
          type: string
        id:
          type: string
        key_prefix:
          description: Non-secret visible prefix (e.g. e2a_acct_… / e2a_agt_…).
          type: string
        last_used_at:
          format: date-time
          type: string
        name:
          type: string
        scope:
          description: account = workspace admin; agent = bound to one inbox.
          enum:
            - account
            - agent
          type: string
      required:
        - id
        - name
        - key_prefix
        - scope
        - created_at
      type: object
    AccountUserView:
      additionalProperties: true
      properties:
        email:
          type: string
        id:
          type: string
      required:
        - id
        - email
      type: object
    AccountView:
      additionalProperties: true
      properties:
        agent_email:
          type: string
        limits:
          $ref: "#/components/schemas/LimitsCapsView"
        plan_code:
          type: string
        scope:
          enum:
            - account
            - agent
          type: string
        upgrade_url:
          type: string
        usage:
          $ref: "#/components/schemas/LimitsUsageView"
        user:
          $ref: "#/components/schemas/AccountUserView"
      required:
        - user
        - scope
        - plan_code
        - limits
        - usage
        - upgrade_url
      type: object
    AgentIdentity:
      additionalProperties: true
      properties:
        created_at:
          format: date-time
          type: string
        deleted_at:
          format: date-time
          type: string
        domain:
          type: string
        domain_verified:
          type: boolean
        email:
          type: string
        inbound_7d:
          format: int64
          type: integer
        inbound_allowlist:
          items:
            type: string
          type:
            - array
            - "null"
        inbound_policy:
          type: string
        inbound_policy_action:
          type: string
        inbound_scan:
          type: string
        inbound_scan_block_threshold:
          format: double
          type: number
        inbound_scan_review_threshold:
          format: double
          type: number
        inbound_scan_sensitivity:
          type: string
        last_delivery_at:
          format: date-time
          type: string
        name:
          type: string
        on_expiry:
          type: string
        outbound_7d:
          format: int64
          type: integer
        outbound_allowlist:
          items:
            type: string
          type:
            - array
            - "null"
        outbound_policy:
          type: string
        outbound_policy_action:
          type: string
        outbound_scan:
          type: string
        outbound_scan_block_threshold:
          format: double
          type: number
        outbound_scan_review_threshold:
          format: double
          type: number
        outbound_scan_sensitivity:
          type: string
        pending_count:
          format: int64
          type: integer
        public:
          type: boolean
        ttl_seconds:
          format: int64
          type: integer
        user_id:
          type: string
        webhook_healthy:
          type: boolean
      required:
        - domain
        - email
        - name
        - domain_verified
        - public
        - created_at
        - user_id
        - ttl_seconds
        - on_expiry
        - inbound_7d
        - outbound_7d
        - pending_count
        - webhook_healthy
        - inbound_policy
        - inbound_policy_action
        - outbound_policy
        - outbound_policy_action
        - inbound_scan
        - inbound_scan_review_threshold
        - inbound_scan_block_threshold
        - outbound_scan
        - outbound_scan_review_threshold
        - outbound_scan_block_threshold
        - inbound_scan_sensitivity
        - outbound_scan_sensitivity
      type: object
    AgentView:
      additionalProperties: true
      properties:
        created_at:
          format: date-time
          type: string
        deleted_at:
          description: When the agent was moved to the trash. Omitted for live agents.
          format: date-time
          type: string
        domain:
          type: string
        domain_verified:
          type: boolean
        email:
          type: string
        name:
          type: string
      required:
        - domain
        - email
        - name
        - domain_verified
        - created_at
      type: object
    ApproveRequest:
      additionalProperties: false
      properties:
        attachments:
          items:
            $ref: "#/components/schemas/Attachment"
          type: array
        bcc:
          items:
            type: string
          type: array
        cc:
          items:
            type: string
          type: array
        html:
          type: string
        subject:
          type: string
        text:
          type: string
        to:
          items:
            type: string
          type: array
      type: object
    Attachment:
      additionalProperties: false
      properties:
        content_type:
          examples:
            - application/pdf
          type: string
        data:
          description: Base64-encoded file content. Each attachment must be ≤ 10 MB decoded; a message may carry at most 10 attachments totaling ≤ 25 MB decoded.
          examples:
            - base64-encoded-content
          type: string
        filename:
          examples:
            - report.pdf
          type: string
      required:
        - filename
        - content_type
        - data
      type: object
    AttachmentMetaView:
      additionalProperties: true
      properties:
        content_type:
          type: string
        filename:
          type: string
        index:
          format: int64
          type: integer
        size_bytes:
          format: int64
          type: integer
      required:
        - index
        - size_bytes
      type: object
    AttachmentView:
      additionalProperties: true
      properties:
        content_type:
          type: string
        data:
          type: string
        download_url:
          type: string
        expires_at:
          format: date-time
          type: string
        filename:
          type: string
        index:
          format: int64
          type: integer
        size_bytes:
          format: int64
          type: integer
      required:
        - index
        - size_bytes
        - download_url
        - expires_at
      type: object
    AuthVerdict:
      additionalProperties: true
      properties:
        dkim:
          $ref: "#/components/schemas/CheckResult"
        dmarc:
          $ref: "#/components/schemas/CheckResult"
        spf:
          $ref: "#/components/schemas/CheckResult"
      required:
        - spf
        - dkim
        - dmarc
      type: object
    CheckResult:
      additionalProperties: true
      properties:
        detail:
          type: string
        status:
          type: string
      required:
        - status
      type: object
    ConversationDetailView:
      additionalProperties: true
      properties:
        first_message_at:
          format: date-time
          type: string
        has_unread:
          type: boolean
        id:
          type: string
        inbound_count:
          format: int64
          type: integer
        labels:
          items:
            type: string
          type: array
        last_message_at:
          format: date-time
          type: string
        latest_from:
          type: string
        latest_subject:
          type: string
        message_count:
          format: int64
          type: integer
        messages:
          items:
            $ref: "#/components/schemas/MessageSummaryView"
          type: array
        outbound_count:
          format: int64
          type: integer
        participants:
          items:
            type: string
          type: array
      required:
        - participants
        - labels
        - messages
        - id
        - last_message_at
        - first_message_at
        - message_count
        - inbound_count
        - outbound_count
        - has_unread
        - latest_subject
        - latest_from
      type: object
    ConversationSummaryView:
      additionalProperties: true
      properties:
        first_message_at:
          format: date-time
          type: string
        has_unread:
          type: boolean
        id:
          type: string
        inbound_count:
          format: int64
          type: integer
        last_message_at:
          format: date-time
          type: string
        latest_from:
          type: string
        latest_subject:
          type: string
        message_count:
          format: int64
          type: integer
        outbound_count:
          format: int64
          type: integer
      required:
        - id
        - last_message_at
        - first_message_at
        - message_count
        - inbound_count
        - outbound_count
        - has_unread
        - latest_subject
        - latest_from
      type: object
    CreateAPIKeyRequest:
      additionalProperties: false
      properties:
        agent_email:
          description: Inbox email to bind the key to; required when scope=agent.
          type: string
        expires_at:
          description: Optional hard expiry (RFC 3339, must be in the future). Omit for a never-expiring key.
          format: date-time
          type: string
        name:
          description: Human label for the key.
          type: string
        scope:
          description: account = workspace admin (default); agent = bound to a single inbox.
          enum:
            - account
            - agent
          type: string
      type: object
    CreateAPIKeyResponse:
      additionalProperties: true
      properties:
        agent_email:
          description: Bound inbox email for agent-scoped keys; omitted for account scope.
          type: string
        created_at:
          format: date-time
          type: string
        expires_at:
          format: date-time
          type: string
        id:
          type: string
        key:
          description: The secret key. Shown once; store it now — it cannot be retrieved later.
          type: string
        key_prefix:
          description: Non-secret visible prefix (e.g. e2a_acct_… / e2a_agt_…).
          type: string
        last_used_at:
          format: date-time
          type: string
        name:
          type: string
        scope:
          description: account = workspace admin; agent = bound to one inbox.
          enum:
            - account
            - agent
          type: string
      required:
        - key
        - id
        - name
        - key_prefix
        - scope
        - created_at
      type: object
    CreateAgentRequest:
      additionalProperties: false
      properties:
        email:
          type: string
        name:
          type: string
      required:
        - email
      type: object
    CreateTemplateRequest:
      additionalProperties: false
      properties:
        alias:
          description: Optional per-user unique handle ([A-Za-z][A-Za-z0-9._-]{0,127}) usable as template_alias on send.
          type: string
        from_starter:
          description: "Copy a starter template (by alias, see GET /v1/starter-templates) verbatim into your library. Mutually exclusive with subject, text and html — the copy is verbatim; edit the created template afterwards. name and alias default to the starter's and may be overridden. Beta: templates are unstable — their shape may change before they are declared stable."
          type: string
        html:
          description: Optional HTML body template source ({{x}} is HTML-escaped, {{{x}}} is raw).
          type: string
        name:
          description: Human-readable template name. Required unless from_starter supplies the default.
          type: string
        subject:
          description: Subject template source ({{variable}} interpolation, no HTML escaping). Required unless from_starter is set.
          type: string
        text:
          description: Plain-text body template source (no HTML escaping). Required unless from_starter is set.
          type: string
      type: object
      x-stability: experimental
    CreateWebhookRequest:
      additionalProperties: false
      properties:
        description:
          type: string
        events:
          description: "Beta: the screening + review-hold events (email.flagged, email.blocked, email.pending_review, email.review_approved, email.review_rejected) are unstable — their payload may change before they are declared stable. All other events are stable."
          items:
            enum:
              - email.received
              - email.sent
              - email.failed
              - email.deferred
              - email.review_approved
              - email.review_rejected
              - domain.sending_verified
              - domain.sending_failed
              - email.delivered
              - email.bounced
              - email.complained
              - domain.suppression_added
              - email.flagged
              - email.blocked
              - email.pending_review
            type: string
          type: array
          x-experimental-values:
            - email.flagged
            - email.blocked
            - email.pending_review
            - email.review_approved
            - email.review_rejected
        filters:
          $ref: "#/components/schemas/WebhookFiltersRequest"
        url:
          type: string
      required:
        - url
        - events
      type: object
    CreateWebhookResponse:
      additionalProperties: true
      properties:
        auto_disabled_at:
          format: date-time
          type: string
        created_at:
          format: date-time
          type: string
        description:
          type: string
        enabled:
          type: boolean
        events:
          description: "The event types this webhook matches. Open set: new event types may be added over time, so treat these as strings and tolerate unknown values. Known values: email.received, email.sent, email.failed, email.deferred, email.delivered, email.bounced, email.complained, email.flagged, email.blocked, email.pending_review, email.review_approved, email.review_rejected, domain.sending_verified, domain.sending_failed, domain.suppression_added. Beta: the screening + review-hold events (email.flagged, email.blocked, email.pending_review, email.review_approved, email.review_rejected) are unstable — their payload may change before they are declared stable."
          items:
            type: string
          type: array
          x-experimental-values:
            - email.flagged
            - email.blocked
            - email.pending_review
            - email.review_approved
            - email.review_rejected
        filters:
          $ref: "#/components/schemas/WebhookFiltersView"
        id:
          type: string
        last_delivered_at:
          format: date-time
          type: string
        signing_secret:
          type: string
        url:
          type: string
      required:
        - signing_secret
        - id
        - url
        - description
        - events
        - filters
        - enabled
        - created_at
      type: object
    DNSRecord:
      additionalProperties: true
      properties:
        name:
          description: Record name (host). The apex domain for ownership/inbound_mx; an FQDN for dkim/mail_from records.
          type: string
        priority:
          description: MX priority. Null for non-MX records.
          format: int64
          type:
            - integer
            - "null"
        purpose:
          description: "What the record is for. Open set; tolerate unknown values. Known values: ownership, inbound_mx, dkim, mail_from_mx, mail_from_spf."
          type: string
        status:
          description: "Per-record verification state. Open set; tolerate unknown values. Known values: verified, pending, missing, failed. Inbound records (ownership, inbound_mx) become verified once inbound verification passes, which requires BOTH the ownership TXT and the inbound MX. Sending records reflect their own SES axis: the dkim record follows the DKIM axis, while mail_from_mx and mail_from_spf follow the custom MAIL FROM axis, so a domain with working DKIM but a broken MAIL FROM (or the reverse) shows exactly which record to fix rather than failing all three. Before SES has reported a per-axis result (pre-provision rows) the sending records fall back to the all-or-nothing sending_status rollup; consult sending_error for the failure reason. The domain-level sending_status field remains the all-or-nothing rollup summary."
          type: string
        type:
          description: DNS record type. MX or TXT.
          type: string
        value:
          description: Record value. For MX records this is the mail-server host only; the priority is in the priority field.
          type: string
      required:
        - type
        - name
        - value
        - priority
        - purpose
        - status
      type: object
    DeleteUserDataResult:
      additionalProperties: true
      properties:
        agents_deleted:
          format: int64
          type: integer
        api_keys_deleted:
          format: int64
          type: integer
        domains_deleted:
          format: int64
          type: integer
        messages_deleted:
          format: int64
          type: integer
        oauth_access_tokens_deleted:
          format: int64
          type: integer
        oauth_auth_codes_deleted:
          format: int64
          type: integer
        oauth_refresh_tokens_deleted:
          format: int64
          type: integer
        sessions_deleted:
          format: int64
          type: integer
        usage_events_deleted:
          format: int64
          type: integer
        usage_summaries_deleted:
          format: int64
          type: integer
        user_deleted:
          type: boolean
      required:
        - usage_events_deleted
        - usage_summaries_deleted
        - messages_deleted
        - agents_deleted
        - domains_deleted
        - api_keys_deleted
        - sessions_deleted
        - user_deleted
      type: object
    DeliveryStatusJSON:
      additionalProperties: true
      properties:
        delivered:
          format: int64
          type: integer
        failed:
          format: int64
          type: integer
        matched_webhooks:
          format: int64
          type: integer
        pending:
          format: int64
          type: integer
      required:
        - matched_webhooks
        - delivered
        - pending
        - failed
      type: object
    DeploymentInfoView:
      additionalProperties: true
      properties:
        public_url:
          type: string
        shared_domain:
          type: string
        slug_registration_enabled:
          type: boolean
        version:
          type: string
      required:
        - version
        - shared_domain
        - slug_registration_enabled
      type: object
    Domain:
      additionalProperties: true
      properties:
        agent_count:
          format: int64
          type: integer
        created_at:
          format: date-time
          type: string
        dkim_public_key:
          type: string
        dkim_selector:
          type: string
        domain:
          type: string
        last_checked_at:
          format: date-time
          type: string
        primary:
          type: boolean
        sending_error:
          type: string
        sending_last_checked_at:
          format: date-time
          type: string
        sending_status:
          type: string
        user_id:
          type: string
        verification_token:
          type: string
        verified:
          type: boolean
        verified_at:
          format: date-time
          type: string
      required:
        - domain
        - verified
        - verification_token
        - created_at
        - primary
        - agent_count
        - sending_status
      type: object
    DomainView:
      additionalProperties: true
      properties:
        agent_count:
          format: int64
          type: integer
        created_at:
          format: date-time
          type: string
        dns_records:
          items:
            $ref: "#/components/schemas/DNSRecord"
          type: array
        domain:
          type: string
        last_checked_at:
          format: date-time
          type: string
        sending_error:
          type: string
        sending_last_checked_at:
          format: date-time
          type: string
        sending_status:
          description: "Async SES sending-identity state (rollup). Open set; tolerate unknown values. Known values: none, pending, verified, failed."
          type: string
        verification_token:
          type: string
        verified:
          type: boolean
        verified_at:
          format: date-time
          type: string
      required:
        - domain
        - verified
        - verification_token
        - dns_records
        - created_at
        - agent_count
        - sending_status
      type: object
    ErrorBody:
      additionalProperties: true
      properties:
        code:
          description: "Machine-branchable error code — the stable discriminator clients switch on. Open set: treat it as a string and tolerate unknown values, since new codes may be added over time (branch on the ones you handle, fall back to the HTTP status otherwise). Known values: invalid_request, unauthorized, forbidden, not_found, conflict, gone, method_not_allowed, payload_too_large, unsupported_media_type, rate_limited, limit_exceeded, internal_error — plus resource-specific codes (e.g. domain_not_verified, invalid_cursor, idempotency_in_flight, idempotency_key_reuse) and the *_not_found / *_exists suffix families. A single canonical code, invalid_request, covers all input-validation failures whether they arrive as 400 (malformed) or 422 (semantically invalid)."
          type: string
        details:
          anyOf:
            - $ref: "#/components/schemas/ValidationErrorDetails"
            - type: object
          description: Optional structured context, polymorphic by code. For invalid_request it is a ValidationErrorDetails ({"fields":[{"location","message"}]}); rate_limited and limit_exceeded carry {"retry_after_seconds"}. Treat it as an open object keyed off code — new codes may introduce new detail shapes.
        message:
          description: Human-readable explanation. Not for branching — use code.
          type: string
        request_id:
          description: Echoes the X-Request-Id response header so a failing call is greppable in logs.
          type: string
      required:
        - code
        - message
      type: object
    ErrorEnvelope:
      additionalProperties: true
      properties:
        error:
          $ref: "#/components/schemas/ErrorBody"
      required:
        - error
      type: object
    EventJSON:
      additionalProperties: true
      properties:
        agent_email:
          type: string
        conversation_id:
          type: string
        created_at:
          format: date-time
          type: string
        data:
          additionalProperties: {}
          type: object
        delivery_status:
          $ref: "#/components/schemas/DeliveryStatusJSON"
        id:
          type: string
        message_id:
          type: string
        schema_version:
          description: Envelope schema version — a semver-ish string label (currently "1").
          type: string
        status:
          description: "Event processing state. Open set; tolerate unknown values. Known values: pending, processed, no_match."
          type: string
        type:
          description: "Event type. Open set: new event types may be added over time, so treat as a string and tolerate unknown values. Known values: email.received, email.sent, email.delivered, email.bounced, email.complained, email.flagged, email.blocked, email.pending_review, email.review_approved, email.review_rejected, domain.sending_verified, domain.sending_failed, domain.suppression_added."
          type: string
      required:
        - id
        - type
        - schema_version
        - created_at
        - status
        - data
      type: object
    FieldError:
      additionalProperties: true
      properties:
        location:
          description: Path-like pointer to the offending field, prefixed with the request part it came from, e.g. body.events, body.items[3].tags, query.limit, path.id. Empty when the failure is not tied to a single field.
          type: string
        message:
          description: Human-readable reason this field is invalid.
          type: string
      required:
        - message
      type: object
    ForwardRequest:
      additionalProperties: false
      properties:
        attachments:
          description: "Additional attachments to include alongside the forwarded message's original attachments, which are carried over automatically. Limits apply to the combined set (originals + these): at most 10 attachments, each ≤ 10 MB decoded, and ≤ 25 MB decoded combined. Exceeding the count → 400 invalid_request; exceeding a size → 413 payload_too_large."
          items:
            $ref: "#/components/schemas/Attachment"
          type: array
        bcc:
          items:
            type: string
          type: array
        cc:
          items:
            type: string
          type: array
        conversation_id:
          type: string
        html:
          type: string
        reply_to:
          description: Sets the Reply-To header — where replies to this message are directed. A single RFC 5322 address, optionally with a display name. Defaults to the sending agent's own address.
          type: string
        text:
          type: string
        to:
          items:
            type: string
          type: array
      required:
        - to
        - text
      type: object
    LimitExceededDetails:
      additionalProperties: true
      properties:
        current:
          description: The account's usage at the time the cap was hit (matches usage.<resource>).
          format: int64
          type: integer
        limit:
          description: The cap that was hit (matches limits.max_<resource>).
          format: int64
          type: integer
        plan_code:
          description: The account's plan label.
          type: string
        resource:
          description: The AccountView usage/limits field stem the cap applies to. Key it to usage.<resource> and limits.max_<resource>.
          enum:
            - agents
            - domains
            - messages_month
            - storage_bytes
          type: string
        upgrade_url:
          description: An upgrade affordance URL, when the operator has configured one.
          type: string
      required:
        - resource
        - limit
        - current
      type: object
    LimitExceededEnvelope:
      additionalProperties: true
      properties:
        error:
          $ref: "#/components/schemas/LimitExceededErrorBody"
      required:
        - error
      type: object
    LimitExceededErrorBody:
      additionalProperties: true
      properties:
        code:
          description: Always limit_exceeded for this response.
          enum:
            - limit_exceeded
          type: string
        details:
          $ref: "#/components/schemas/LimitExceededDetails"
        message:
          type: string
        request_id:
          type: string
      required:
        - code
        - message
        - details
      type: object
    LimitsCapsView:
      additionalProperties: true
      properties:
        max_agents:
          format: int64
          type: integer
        max_domains:
          format: int64
          type: integer
        max_messages_month:
          format: int64
          type: integer
        max_storage_bytes:
          format: int64
          type: integer
      required:
        - max_agents
        - max_domains
        - max_messages_month
        - max_storage_bytes
      type: object
    LimitsUsageView:
      additionalProperties: true
      properties:
        agents:
          format: int64
          type: integer
        domains:
          format: int64
          type: integer
        messages_month:
          format: int64
          type: integer
        storage_bytes:
          format: int64
          type: integer
      required:
        - agents
        - domains
        - messages_month
        - storage_bytes
      type: object
    Message:
      additionalProperties: true
      properties:
        agent_email:
          type: string
        approval_expires_at:
          format: date-time
          type: string
        attachments: {}
        auth:
          $ref: "#/components/schemas/Result"
        auth_headers:
          additionalProperties:
            type: string
          type: object
        bcc:
          items:
            type: string
          type:
            - array
            - "null"
        cc:
          items:
            type: string
          type:
            - array
            - "null"
        conversation_id:
          type: string
        created_at:
          format: date-time
          type: string
        deleted_at:
          format: date-time
          type: string
        delivered_to:
          type: string
        delivery_detail:
          type: string
        delivery_status:
          type: string
        direction:
          type: string
        edited:
          type: boolean
        email_message_id:
          type: string
        expires_at:
          format: date-time
          type: string
        flag_reason:
          type: string
        flagged:
          type: boolean
        from:
          type: string
        html:
          type: string
        id:
          type: string
        labels:
          items:
            type: string
          type:
            - array
            - "null"
        method:
          type: string
        provider_message_id:
          type: string
        raw_message:
          contentEncoding: base64
          type: string
        read_status:
          type: string
        rejection_reason:
          type: string
        reply_to:
          items:
            type: string
          type:
            - array
            - "null"
        review_reason:
          type: string
        reviewed_at:
          format: date-time
          type: string
        reviewed_by_name:
          type: string
        reviewed_by_user_id:
          type: string
        scan_action:
          type: string
        scan_score:
          format: double
          type: number
        sent_as:
          type: string
        size_bytes:
          format: int64
          type: integer
        status:
          type: string
        subject:
          type: string
        text:
          type: string
        to:
          items:
            type: string
          type:
            - array
            - "null"
        type:
          type: string
        webhook_attempts:
          format: int64
          type: integer
        webhook_error:
          type: string
        webhook_status:
          type: string
      required:
        - id
        - agent_email
        - direction
        - from
        - delivered_to
        - subject
        - created_at
        - expires_at
      type: object
    MessageBodyView:
      additionalProperties: true
      properties:
        html:
          type: string
        text:
          type: string
      type: object
    MessageParsedView:
      additionalProperties: true
      properties:
        html:
          type: string
        text:
          type: string
        truncated:
          type: boolean
      required:
        - text
        - truncated
      type: object
    MessageSummaryView:
      additionalProperties: true
      properties:
        auth:
          $ref: "#/components/schemas/AuthVerdict"
        cc:
          items:
            type: string
          type: array
        conversation_id:
          type: string
        created_at:
          format: date-time
          type: string
        deleted_at:
          description: When the message was moved to the trash. Omitted for live messages.
          format: date-time
          type: string
        delivered_to:
          description: The envelope Delivered-To address — this delivery's per-agent target (the mailbox that actually received this row), distinct from the To header (the to array).
          type: string
        delivery_detail:
          type: string
        delivery_status:
          description: "Outbound delivery rollup (worst recipient status by precedence; outbound only). Open set; tolerate unknown values. Known values: accepted, sending, sent, delivered, deferred, bounced, complained, failed. Lifecycle: accepted → sending → sent → delivered | deferred | bounced | complained | failed. (Legacy 'queued' is superseded by 'accepted'.)"
          type: string
        direction:
          enum:
            - inbound
            - outbound
          type: string
        flag_reason:
          type: string
        flagged:
          type: boolean
        from:
          type: string
        id:
          type: string
        labels:
          items:
            type: string
          type: array
        read_status:
          type: string
        reply_to:
          description: The parsed Reply-To header of an inbound message. Populated for inbound only; always empty for outbound (a Reply-To you SET on a send is a request-side field and is not echoed back here).
          items:
            type: string
          type: array
        review_status:
          description: "Review-hold lifecycle (outbound only). Open set; tolerate unknown values. Known values: pending_review, sent, review_rejected, review_expired_approved, review_expired_rejected. Note: an APPROVED outbound hold reads as sent here — the message view intentionally collapses the approved outcome into the delivery lifecycle. The distinct review_approved spelling appears only in the approve result (SendResultView.status, for inbound release) and the email.review_approved webhook event, not in this field."
          type: string
        sent_as:
          description: "From identity used at relay accept time (outbound only). Open set; tolerate unknown values. Known values: own_address, relay."
          type: string
        size_bytes:
          format: int64
          type: integer
        subject:
          type: string
        to:
          items:
            type: string
          type: array
        webhook_error:
          type: string
        webhook_status:
          type: string
      required:
        - id
        - direction
        - from
        - to
        - delivered_to
        - subject
        - read_status
        - labels
        - created_at
      type: object
    MessageView:
      additionalProperties: true
      properties:
        attachments:
          items:
            $ref: "#/components/schemas/AttachmentMetaView"
          type: array
        auth:
          $ref: "#/components/schemas/AuthVerdict"
        auth_headers:
          additionalProperties:
            type: string
          type: object
        body:
          $ref: "#/components/schemas/MessageBodyView"
        cc:
          items:
            type: string
          type: array
        conversation_id:
          type: string
        created_at:
          format: date-time
          type: string
        deleted_at:
          description: When the message was moved to the trash. Omitted for live messages.
          format: date-time
          type: string
        delivered_to:
          description: The envelope Delivered-To address — this delivery's per-agent target (the mailbox that actually received this row), distinct from the To header (the to array).
          type: string
        delivery_detail:
          type: string
        delivery_status:
          description: "Outbound delivery rollup (worst recipient status by precedence; outbound only). Open set; tolerate unknown values. Known values: accepted, sending, sent, delivered, deferred, bounced, complained, failed. Lifecycle: accepted → sending → sent → delivered | deferred | bounced | complained | failed. (Legacy 'queued' is superseded by 'accepted'.)"
          type: string
        direction:
          enum:
            - inbound
            - outbound
          type: string
        flag_reason:
          type: string
        flagged:
          type: boolean
        from:
          type: string
        id:
          type: string
        labels:
          items:
            type: string
          type: array
        parsed:
          $ref: "#/components/schemas/MessageParsedView"
        raw_message:
          contentEncoding: base64
          type: string
        read_status:
          type: string
        reply_to:
          description: The parsed Reply-To header of an inbound message. Populated for inbound only; always empty for outbound (a Reply-To you SET on a send is a request-side field on the send/reply/forward body and is not echoed back here).
          items:
            type: string
          type: array
        review_status:
          description: "Review-hold lifecycle (outbound only). Open set; tolerate unknown values. Known values: pending_review, sent, review_rejected, review_expired_approved, review_expired_rejected. Note: an APPROVED outbound hold reads as sent here — the message view intentionally collapses the approved outcome into the delivery lifecycle. The distinct review_approved spelling appears only in the approve result (SendResultView.status, for inbound release) and the email.review_approved webhook event, not in this field."
          type: string
        sent_as:
          description: "From identity used at relay accept time (outbound only). Open set; tolerate unknown values. Known values: own_address, relay."
          type: string
        size_bytes:
          format: int64
          type: integer
        subject:
          type: string
        to:
          items:
            type: string
          type: array
        webhook_error:
          type: string
        webhook_status:
          type: string
      required:
        - id
        - from
        - to
        - cc
        - reply_to
        - delivered_to
        - subject
        - conversation_id
        - direction
        - read_status
        - labels
        - created_at
        - raw_message
        - attachments
      type: object
    OAuthConnectionEntry:
      additionalProperties: true
      properties:
        agent_email:
          type: string
        client_id:
          type: string
        client_name:
          type: string
        expires_at:
          format: date-time
          type: string
        issued_at:
          format: date-time
          type: string
        revoked_at:
          format: date-time
          type: string
        scope:
          type: string
      required:
        - client_id
        - client_name
        - agent_email
        - scope
        - issued_at
      type: object
    PageAPIKeyView:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/APIKeyView"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
    PageAgentView:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/AgentView"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
    PageConversationSummaryView:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/ConversationSummaryView"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
    PageDomainView:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/DomainView"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
    PageEventJSON:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/EventJSON"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
    PageMessageSummaryView:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/MessageSummaryView"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
    PageReviewView:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/ReviewView"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
    PageStarterTemplateView:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/StarterTemplateView"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
      x-stability: experimental
    PageSuppression:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/Suppression"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
    PageTemplateSummaryView:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/TemplateSummaryView"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
      x-stability: experimental
    PageWebhookDeliveryView:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/WebhookDeliveryView"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
    PageWebhookView:
      additionalProperties: true
      properties:
        items:
          items:
            $ref: "#/components/schemas/WebhookView"
          type: array
        next_cursor:
          type:
            - string
            - "null"
      required:
        - items
        - next_cursor
      type: object
    ProtectionConfigRequest:
      additionalProperties: false
      properties:
        holds:
          $ref: "#/components/schemas/ProtectionHoldsRequest"
        inbound:
          $ref: "#/components/schemas/ProtectionDirectionRequest"
        outbound:
          $ref: "#/components/schemas/ProtectionDirectionRequest"
      required:
        - inbound
        - outbound
        - holds
      type: object
      x-stability: experimental
    ProtectionConfigView:
      additionalProperties: true
      properties:
        holds:
          $ref: "#/components/schemas/ProtectionHoldsView"
        inbound:
          $ref: "#/components/schemas/ProtectionDirectionView"
        outbound:
          $ref: "#/components/schemas/ProtectionDirectionView"
      required:
        - inbound
        - outbound
        - holds
      type: object
      x-stability: experimental
    ProtectionDirectionRequest:
      additionalProperties: false
      properties:
        gate:
          $ref: "#/components/schemas/ProtectionGateRequest"
        scan:
          $ref: "#/components/schemas/ProtectionScanRequest"
      required:
        - gate
        - scan
      type: object
      x-stability: experimental
    ProtectionDirectionView:
      additionalProperties: true
      properties:
        gate:
          $ref: "#/components/schemas/ProtectionGateView"
        scan:
          $ref: "#/components/schemas/ProtectionScanView"
      required:
        - gate
        - scan
      type: object
      x-stability: experimental
    ProtectionEventExportEntry:
      additionalProperties: true
      properties:
        action:
          type: string
        agent_email:
          type: string
        created_at:
          format: date-time
          type: string
        detector:
          type: string
        direction:
          type: string
        id:
          type: string
        message_id:
          type: string
        peer_address:
          type: string
        reason:
          type: string
        scan_score:
          format: double
          type: number
        source:
          type: string
      required:
        - id
        - message_id
        - agent_email
        - direction
        - source
        - reason
        - action
        - created_at
      type: object
    ProtectionGateRequest:
      additionalProperties: false
      properties:
        action:
          default: flag
          description: "What a gate non-match does: flag (deliver + annotate), review (hold), block."
          enum:
            - flag
            - review
            - block
          type: string
        allowlist:
          description: "Addresses (allowlist) or domains (domain) the gate trusts; ignored for open. Inbound: matched against the message From AS PRESENTED — a match does not by itself prove the sender is authentic (a forged From that fails SPF/DKIM/DMARC can still match). For spoofing-sensitive trust, also check the message authentication result."
          items:
            type: string
          maxItems: 1000
          type: array
        policy:
          default: open
          description: "Trust gate: open (all), domain (listed domains), allowlist (listed addresses)."
          enum:
            - open
            - allowlist
            - domain
          type: string
      type: object
      x-stability: experimental
    ProtectionGateView:
      additionalProperties: true
      properties:
        action:
          default: flag
          description: "What a gate non-match does: flag (deliver + annotate), review (hold), block."
          enum:
            - flag
            - review
            - block
          type: string
        allowlist:
          description: "Addresses (allowlist) or domains (domain) the gate trusts; ignored for open. Inbound: matched against the message From AS PRESENTED — a match does not by itself prove the sender is authentic (a forged From that fails SPF/DKIM/DMARC can still match). For spoofing-sensitive trust, also check the message authentication result."
          items:
            type: string
          maxItems: 1000
          type: array
        policy:
          default: open
          description: "Trust gate: open (all), domain (listed domains), allowlist (listed addresses)."
          enum:
            - open
            - allowlist
            - domain
          type: string
      type: object
      x-stability: experimental
    ProtectionHoldsRequest:
      additionalProperties: false
      properties:
        on_expiry:
          default: reject
          description: What happens to a held item when its TTL expires.
          enum:
            - approve
            - reject
          type: string
        ttl_seconds:
          default: 604800
          description: How long a held item waits before its on_expiry action fires.
          format: int64
          minimum: 0
          type: integer
      type: object
      x-stability: experimental
    ProtectionHoldsView:
      additionalProperties: true
      properties:
        on_expiry:
          default: reject
          description: What happens to a held item when its TTL expires.
          enum:
            - approve
            - reject
          type: string
        ttl_seconds:
          default: 604800
          description: How long a held item waits before its on_expiry action fires.
          format: int64
          minimum: 0
          type: integer
      type: object
      x-stability: experimental
    ProtectionScanRequest:
      additionalProperties: false
      properties:
        sensitivity:
          default: "off"
          description: "Content-scan sensitivity: off disables; low|medium|high increase aggressiveness."
          enum:
            - "off"
            - low
            - medium
            - high
          type: string
      type: object
      x-stability: experimental
    ProtectionScanView:
      additionalProperties: true
      properties:
        sensitivity:
          default: "off"
          description: "Content-scan sensitivity: off disables; low|medium|high increase aggressiveness."
          enum:
            - "off"
            - low
            - medium
            - high
          type: string
      type: object
      x-stability: experimental
    RateLimitedDetails:
      additionalProperties: true
      properties:
        retry_after_seconds:
          description: Seconds to wait before retrying; mirrors the Retry-After response header. Always ≥ 1.
          format: int64
          type: integer
      required:
        - retry_after_seconds
      type: object
    RateLimitedEnvelope:
      additionalProperties: true
      properties:
        error:
          $ref: "#/components/schemas/RateLimitedErrorBody"
      required:
        - error
      type: object
    RateLimitedErrorBody:
      additionalProperties: true
      properties:
        code:
          description: Always rate_limited for this response.
          enum:
            - rate_limited
          type: string
        details:
          $ref: "#/components/schemas/RateLimitedDetails"
        message:
          type: string
        request_id:
          type: string
      required:
        - code
        - message
        - details
      type: object
    RedeliverDelivery:
      additionalProperties: true
      properties:
        delivery_id:
          type: string
        reason:
          type: string
        status:
          description: "Open set; tolerate unknown values. Known values: pending (replay scheduled), skipped (could not enqueue — see reason)."
          type: string
        webhook_id:
          type: string
      required:
        - webhook_id
        - status
      type: object
    RedeliverEventRequest:
      additionalProperties: false
      properties:
        webhook_id:
          type: string
      type: object
    RedeliverView:
      additionalProperties: true
      properties:
        deliveries:
          items:
            $ref: "#/components/schemas/RedeliverDelivery"
          type: array
        delivery_id:
          type: string
        event_id:
          type: string
        status:
          description: "Open set; tolerate unknown values. Known values: pending (single-webhook replay), scheduled (bulk fan-out)."
          type: string
        webhook_id:
          type: string
      required:
        - event_id
        - status
      type: object
    RegisterDomainRequest:
      additionalProperties: false
      properties:
        domain:
          type: string
      required:
        - domain
      type: object
    RejectRequest:
      additionalProperties: false
      properties:
        reason:
          type: string
      type: object
    RejectResultView:
      additionalProperties: true
      properties:
        message_id:
          type: string
        rejection_reason:
          type: string
        status:
          type: string
      required:
        - status
        - message_id
      type: object
    RenderedTemplateView:
      additionalProperties: true
      properties:
        html:
          type: string
        subject:
          type: string
        text:
          type: string
      required:
        - subject
        - text
      type: object
      x-stability: experimental
    ReplyRequest:
      additionalProperties: false
      properties:
        attachments:
          description: "File attachments (base64 in each item's data). Limits: at most 10 attachments, each ≤ 10 MB decoded, and ≤ 25 MB decoded combined. Exceeding the count → 400 invalid_request; exceeding a size → 413 payload_too_large."
          items:
            $ref: "#/components/schemas/Attachment"
          type: array
        bcc:
          items:
            type: string
          type: array
        cc:
          items:
            type: string
          type: array
        conversation_id:
          type: string
        html:
          type: string
        reply_all:
          type: boolean
        reply_to:
          description: Sets the Reply-To header — where replies to this message are directed. A single RFC 5322 address, optionally with a display name. Defaults to the sending agent's own address.
          type: string
        text:
          type: string
      required:
        - text
      type: object
    Result:
      additionalProperties: true
      properties:
        dkim:
          $ref: "#/components/schemas/CheckResult"
        dmarc:
          $ref: "#/components/schemas/CheckResult"
        spf:
          $ref: "#/components/schemas/CheckResult"
      required:
        - spf
        - dkim
        - dmarc
      type: object
    ReviewView:
      additionalProperties: true
      properties:
        agent_email:
          description: The inbox (agent address) the held message belongs to.
          type: string
        conversation_id:
          type: string
        created_at:
          format: date-time
          type: string
        direction:
          enum:
            - inbound
            - outbound
          type: string
        flag_reason:
          type: string
        flagged:
          type: boolean
        from:
          type: string
        id:
          description: The review's id. This is the SAME value as the held message's id (msg_…) — a review IS the held message pending approval, so GET /v1/reviews/{id} and the message id are interchangeable. Intentional and stable.
          type: string
        review_status:
          description: Hold state of this queue item. Open set; tolerate unknown values. Currently always pending_review (the queue lists held items).
          type: string
        subject:
          type: string
        to:
          items:
            type: string
          type: array
      required:
        - id
        - agent_email
        - direction
        - from
        - to
        - subject
        - review_status
        - created_at
      type: object
    RotateSecretResponse:
      additionalProperties: true
      properties:
        previous_secret_expires_at:
          format: date-time
          type: string
        signing_secret:
          type: string
      required:
        - signing_secret
        - previous_secret_expires_at
      type: object
    SendEmailRequest:
      additionalProperties: false
      properties:
        attachments:
          description: "File attachments (base64 in each item's data). Limits: at most 10 attachments, each ≤ 10 MB decoded, and ≤ 25 MB decoded combined. Exceeding the count → 400 invalid_request; exceeding a size → 413 payload_too_large."
          items:
            $ref: "#/components/schemas/Attachment"
          type: array
        bcc:
          items:
            type: string
          type: array
        cc:
          items:
            type: string
          type: array
        conversation_id:
          type: string
        html:
          description: Literal HTML body. Mutually exclusive with template_id/template_alias.
          type: string
        reply_to:
          description: Sets the Reply-To header — where replies to this message are directed. A single RFC 5322 address, optionally with a display name (e.g. "Support <support@acme.com>"). Defaults to the sending agent's own address.
          type: string
        subject:
          description: Literal subject. Required unless a template reference is used (mutually exclusive with template_id/template_alias).
          type: string
        template_alias:
          description: "Send using a stored template resolved by its per-user alias. Mutually exclusive with template_id and with literal subject/body/html_body. Beta: templates are unstable — their shape may change before they are declared stable."
          type: string
          x-stability: experimental
        template_data:
          additionalProperties: {}
          description: "Variables for the referenced template ({{name}}, dot paths into nested objects). Missing variables render as empty strings. Beta: templates are unstable — their shape may change before they are declared stable."
          type: object
          x-stability: experimental
        template_id:
          description: "Send using a stored template (rendered server-side, before any review hold). Mutually exclusive with template_alias and with literal subject/body/html_body. Beta: templates are unstable — their shape may change before they are declared stable."
          type: string
          x-stability: experimental
        text:
          description: Literal plain-text body. Required unless a template reference is used (mutually exclusive with template_id/template_alias).
          type: string
        to:
          items:
            type: string
          type: array
      required:
        - to
      type: object
    SendResultView:
      additionalProperties: true
      properties:
        approval_expires_at:
          format: date-time
          type: string
        edited:
          type: boolean
        message_id:
          type: string
        method:
          description: "Send transport. Open set; tolerate unknown values. Known values: smtp, loopback."
          type: string
        provider_message_id:
          description: Upstream provider (SES) id. Optional/absent until the message is actually sent — an accepted-but-not-yet-sent message has no provider id.
          type: string
        sent_as:
          description: "From identity used. Open set; tolerate unknown values. Known values: own_address, relay."
          type: string
        status:
          description: "Outcome. Open set; tolerate unknown values. Known values: accepted, sent, pending_review, review_approved, failed. accepted = durably persisted and queued for submission (async pipeline); the terminal outcome arrives via webhook events (email.sent / email.failed) or GET /v1/messages/{id}. failed = terminal failure. Always branch on this field, not the HTTP status code."
          type: string
      required:
        - status
        - message_id
      type: object
    StarterTemplateDetailView:
      additionalProperties: true
      properties:
        alias:
          type: string
        description:
          type: string
        html:
          description: The HTML part's template source.
          type: string
        name:
          type: string
        subject:
          description: Subject template source ({{variable}} interpolation).
          type: string
        text:
          description: The plain-text part's template source.
          type: string
        variables:
          items:
            $ref: "#/components/schemas/StarterTemplateVariableView"
          type: array
        version:
          type: string
      required:
        - text
        - html
        - alias
        - name
        - description
        - version
        - subject
        - variables
      type: object
      x-stability: experimental
    StarterTemplateVariableView:
      additionalProperties: true
      properties:
        description:
          type: string
        example:
          description: A realistic sample value, usable as template_data for previews.
          type: string
        name:
          type: string
        raw:
          description: Raw ({{{...}}}) slots accept pre-rendered HTML fragments inserted without escaping. Never feed untrusted input to raw slots.
          type: boolean
        required:
          description: Required variables should always be supplied; optional ones render as empty strings when absent.
          type: boolean
      required:
        - name
        - required
        - raw
        - description
        - example
      type: object
      x-stability: experimental
    StarterTemplateView:
      additionalProperties: true
      properties:
        alias:
          type: string
        description:
          type: string
        name:
          type: string
        subject:
          description: Subject template source ({{variable}} interpolation).
          type: string
        variables:
          items:
            $ref: "#/components/schemas/StarterTemplateVariableView"
          type: array
        version:
          type: string
      required:
        - alias
        - name
        - description
        - version
        - subject
        - variables
      type: object
      x-stability: experimental
    Suppression:
      additionalProperties: true
      properties:
        address:
          type: string
        created_at:
          format: date-time
          type: string
        reason:
          type: string
        source:
          type: string
        source_message_id:
          type: string
      required:
        - address
        - source
        - created_at
      type: object
    SuppressionExportEntry:
      additionalProperties: true
      properties:
        address:
          type: string
        created_at:
          format: date-time
          type: string
        reason:
          type: string
        source:
          type: string
      required:
        - address
        - source
        - created_at
      type: object
    TemplatePartError:
      additionalProperties: true
      properties:
        message:
          type: string
        part:
          description: "Which part failed. Known values: subject, text, html."
          type: string
      required:
        - part
        - message
      type: object
      x-stability: experimental
    TemplateSummaryView:
      additionalProperties: true
      properties:
        alias:
          description: Optional per-user unique handle usable as template_alias on send.
          type: string
        created_at:
          format: date-time
          type: string
        id:
          type: string
        name:
          type: string
        subject:
          type: string
        updated_at:
          format: date-time
          type: string
      required:
        - id
        - name
        - subject
        - created_at
        - updated_at
      type: object
      x-stability: experimental
    TemplateView:
      additionalProperties: true
      properties:
        alias:
          description: Optional per-user unique handle usable as template_alias on send.
          type: string
        created_at:
          format: date-time
          type: string
        from_starter_alias:
          description: "The starter template this was copied from (read-only, set by from_starter creates). Beta: templates are unstable — their shape may change before they are declared stable."
          type: string
        from_starter_version:
          description: "The starter catalog version at copy time (read-only, set by from_starter creates). Beta: templates are unstable — their shape may change before they are declared stable."
          type: string
        html:
          description: The optional HTML part's template source.
          type: string
        id:
          type: string
        name:
          type: string
        subject:
          type: string
        text:
          description: The plain-text part's template source.
          type: string
        updated_at:
          format: date-time
          type: string
      required:
        - id
        - name
        - subject
        - text
        - created_at
        - updated_at
      type: object
      x-stability: experimental
    TestWebhookRequest:
      additionalProperties: false
      properties:
        data:
          additionalProperties: {}
          type: object
        type:
          enum:
            - email.received
            - email.sent
            - email.failed
            - email.deferred
            - email.review_approved
            - email.review_rejected
            - domain.sending_verified
            - domain.sending_failed
            - email.delivered
            - email.bounced
            - email.complained
            - domain.suppression_added
            - email.flagged
            - email.blocked
            - email.pending_review
          type: string
      type: object
    TestWebhookResponse:
      additionalProperties: true
      properties:
        delivery_id:
          type: string
      required:
        - delivery_id
      type: object
    UpdateAgentRequest:
      additionalProperties: false
      properties:
        name:
          description: New display name for the agent (a UI label; the agent's identity is its email).
          maxLength: 200
          type: string
      type: object
    UpdateMessageRequest:
      additionalProperties: false
      properties:
        add_labels:
          items:
            type: string
          type: array
        remove_labels:
          items:
            type: string
          type: array
      type: object
    UpdateMessageResultView:
      additionalProperties: true
      properties:
        labels:
          items:
            type: string
          type: array
        message_id:
          type: string
      required:
        - message_id
        - labels
      type: object
    UpdateTemplateRequest:
      additionalProperties: false
      properties:
        alias:
          description: Set to "" to clear the alias.
          type: string
        html:
          description: Set to "" to remove the HTML part.
          type: string
        name:
          type: string
        subject:
          type: string
        text:
          type: string
      type: object
      x-stability: experimental
    UpdateWebhookRequest:
      additionalProperties: false
      properties:
        description:
          type: string
        enabled:
          type: boolean
        events:
          description: "Beta: the screening + review-hold events (email.flagged, email.blocked, email.pending_review, email.review_approved, email.review_rejected) are unstable — their payload may change before they are declared stable. All other events are stable."
          items:
            enum:
              - email.received
              - email.sent
              - email.failed
              - email.deferred
              - email.review_approved
              - email.review_rejected
              - domain.sending_verified
              - domain.sending_failed
              - email.delivered
              - email.bounced
              - email.complained
              - domain.suppression_added
              - email.flagged
              - email.blocked
              - email.pending_review
            type: string
          type: array
          x-experimental-values:
            - email.flagged
            - email.blocked
            - email.pending_review
            - email.review_approved
            - email.review_rejected
        filters:
          $ref: "#/components/schemas/WebhookFiltersRequest"
        url:
          type: string
      type: object
    UsageEventEntry:
      additionalProperties: true
      properties:
        agent_email:
          type: string
        created_at:
          format: date-time
          type: string
        direction:
          type: string
        domain:
          type: string
        id:
          type: string
        type:
          type: string
      required:
        - id
        - agent_email
        - domain
        - direction
        - type
        - created_at
      type: object
    UserExport:
      additionalProperties: true
      properties:
        agents:
          items:
            $ref: "#/components/schemas/AgentIdentity"
          type: array
        api_keys:
          items:
            $ref: "#/components/schemas/APIKeyExportEntry"
          type: array
        domains:
          items:
            $ref: "#/components/schemas/Domain"
          type: array
        generated_at:
          format: date-time
          type: string
        messages:
          items:
            $ref: "#/components/schemas/Message"
          type: array
        oauth_connections:
          items:
            $ref: "#/components/schemas/OAuthConnectionEntry"
          type: array
        protection_events:
          items:
            $ref: "#/components/schemas/ProtectionEventExportEntry"
          type: array
        schema_version:
          type: string
        suppressions:
          items:
            $ref: "#/components/schemas/SuppressionExportEntry"
          type: array
        usage_events:
          items:
            $ref: "#/components/schemas/UsageEventEntry"
          type: array
        user:
          $ref: "#/components/schemas/UserExportUser"
      required:
        - generated_at
        - schema_version
        - user
        - domains
        - agents
        - api_keys
        - messages
        - suppressions
        - protection_events
      type: object
    UserExportUser:
      additionalProperties: true
      properties:
        created_at:
          format: date-time
          type: string
        email:
          type: string
        id:
          type: string
        name:
          type: string
      required:
        - id
        - email
        - name
        - created_at
      type: object
    ValidateTemplateRequest:
      additionalProperties: false
      properties:
        html:
          type: string
        subject:
          type: string
        test_data:
          additionalProperties: {}
          description: Sample template_data to render the preview with. Missing variables render as empty strings.
          type: object
        text:
          type: string
      type: object
      x-stability: experimental
    ValidateTemplateResponse:
      additionalProperties: true
      properties:
        errors:
          items:
            $ref: "#/components/schemas/TemplatePartError"
          type: array
        rendered:
          $ref: "#/components/schemas/RenderedTemplateView"
          description: Rendered preview against test_data (or empty data). Present only when valid.
        suggested_data:
          additionalProperties: {}
          description: A placeholder value for every variable the source references — a starting point for template_data. Dot-path variables ({{user.name}}) emit NESTED objects, matching how the renderer resolves them.
          type: object
        valid:
          type: boolean
      required:
        - valid
        - errors
      type: object
      x-stability: experimental
    ValidationErrorDetails:
      additionalProperties: true
      properties:
        fields:
          description: The fields that failed validation. May be empty when the failure is request-wide rather than field-specific.
          items:
            $ref: "#/components/schemas/FieldError"
          type:
            - array
            - "null"
      required:
        - fields
      type: object
    VerifyDomainView:
      additionalProperties: true
      properties:
        dkim:
          description: "Live DKIM probe state. Known values: found, missing, deferred, mismatch. 'mismatch' means a DKIM record IS published at the selector but its key doesn't match the issued one — almost always a truncated/clipped TXT (the value is ~400 chars and must be published in full, ending in 'AQAB'). On 'mismatch', re-publish the complete DKIM record; do not just wait."
          type: string
        domain:
          type: string
        mx:
          type: string
        spf:
          type: string
        verified:
          type: boolean
        verified_at:
          format: date-time
          type: string
      required:
        - domain
        - verified
      type: object
    WebhookDeliveryView:
      additionalProperties: true
      properties:
        attempts:
          format: int64
          type: integer
        created_at:
          format: date-time
          type: string
        id:
          type: string
        last_attempt_at:
          format: date-time
          type: string
        last_error:
          type: string
        last_status_code:
          format: int64
          type: integer
        next_retry_at:
          format: date-time
          type: string
        status:
          description: "Delivery state. Open set; tolerate unknown values. Known values: pending, delivered, failed."
          type: string
        type:
          description: "The event type that triggered this delivery. Open set: new event types may be added, so treat as a string and tolerate unknown values. Known values are the webhook event catalog (email.received, email.sent, email.failed, email.deferred, email.delivered, …, domain.*)."
          type: string
      required:
        - id
        - type
        - status
        - attempts
        - next_retry_at
        - created_at
      type: object
    WebhookFiltersRequest:
      additionalProperties: false
      properties:
        agent_emails:
          items:
            type: string
          type: array
        conversation_ids:
          items:
            type: string
          type: array
        labels:
          items:
            type: string
          type: array
      type: object
    WebhookFiltersView:
      additionalProperties: true
      properties:
        agent_emails:
          items:
            type: string
          type: array
        conversation_ids:
          items:
            type: string
          type: array
        labels:
          items:
            type: string
          type: array
      type: object
    WebhookView:
      additionalProperties: true
      properties:
        auto_disabled_at:
          format: date-time
          type: string
        created_at:
          format: date-time
          type: string
        description:
          type: string
        enabled:
          type: boolean
        events:
          description: "The event types this webhook matches. Open set: new event types may be added over time, so treat these as strings and tolerate unknown values. Known values: email.received, email.sent, email.failed, email.deferred, email.delivered, email.bounced, email.complained, email.flagged, email.blocked, email.pending_review, email.review_approved, email.review_rejected, domain.sending_verified, domain.sending_failed, domain.suppression_added. Beta: the screening + review-hold events (email.flagged, email.blocked, email.pending_review, email.review_approved, email.review_rejected) are unstable — their payload may change before they are declared stable."
          items:
            type: string
          type: array
          x-experimental-values:
            - email.flagged
            - email.blocked
            - email.pending_review
            - email.review_approved
            - email.review_rejected
        filters:
          $ref: "#/components/schemas/WebhookFiltersView"
        id:
          type: string
        last_delivered_at:
          format: date-time
          type: string
        url:
          type: string
      required:
        - id
        - url
        - description
        - events
        - filters
        - enabled
        - created_at
      type: object
  securitySchemes:
    bearer:
      description: "API key (e2a_…) or OAuth 2.1 access token, sent as `Authorization: Bearer <token>`."
      scheme: bearer
      type: http
info:
  description: |-
    e2a — authenticated email gateway for AI agents. v1 contract.

    ## Stability policy

    The v1 surface is stable and evolves **additively only**: new endpoints, new optional request fields, new response fields, and new values in open string sets (event types, statuses) may appear at any time without a version bump. Clients MUST tolerate unknown response fields and unknown values in open string sets. This is machine-readable in the schemas: response schemas declare `additionalProperties: true`; request schemas stay strict (`additionalProperties: false` — an unknown request field is rejected with 422).

    Operations and schemas marked `x-stability: experimental` are exempt from this freeze and may change or be removed without a major version. A field marked `x-experimental-values` is itself stable, but the listed values (and their event payloads) are experimental. Everything not marked experimental is stable.

    Removing or changing stable surface only happens on a new major version path (/v2); deprecations are announced ahead of time via `deprecated: true` in this document and keep working within v1.
  title: e2a API
  version: 1.0.0
openapi: 3.1.0
paths:
  /v1/account:
    delete:
      description: Permanently deletes the account and cascades all owned data. Requires ?confirm=DELETE.
      operationId: deleteAccount
      parameters:
        - description: Must be the literal DELETE — this action is irreversible.
          explode: false
          in: query
          name: confirm
          required: true
          schema:
            description: Must be the literal DELETE — this action is irreversible.
            enum:
              - DELETE
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteUserDataResult"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Delete your account + all data (irreversible)
      tags:
        - account
    get:
      description: The authenticated principal's identity (user + scope; agent_email for agent-scoped credentials), plan caps, and current usage. Works for both account- and agent-scoped credentials. (Deployment discovery — shared domain, slug registration — is the separate public GET /v1/info.)
      operationId: getAccount
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: "Get account: identity + plan limits + usage (whoami)"
      tags:
        - account
  /v1/account/api-keys:
    get:
      description: "API keys for the account (metadata only — secrets are shown once, at creation). Account scope only: an agent-scoped credential cannot manage keys."
      operationId: listApiKeys
      parameters:
        - description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
            type: string
        - description: Maximum number of items to return (1-100).
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            description: Maximum number of items to return (1-100).
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageAPIKeyView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List API keys
      tags:
        - account
    post:
      description: Mint a new API key; the plaintext key is returned once. scope=account is workspace admin (agent/domain/key management); scope=agent binds the key to one inbox so it can act only as that agent. Account scope only.
      operationId: createApiKey
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAPIKeyRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateAPIKeyResponse"
          description: Created
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Create an API key
      tags:
        - account
  /v1/account/api-keys/{id}:
    delete:
      description: Revoke a key by id. Integrations using it stop authenticating immediately. Account scope only. Requires ?confirm=DELETE.
      operationId: deleteApiKey
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
        - description: Must be the literal DELETE — this action is irreversible.
          explode: false
          in: query
          name: confirm
          required: true
          schema:
            description: Must be the literal DELETE — this action is irreversible.
            enum:
              - DELETE
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Revoke an API key
      tags:
        - account
  /v1/account/export:
    get:
      description: A JSON dump of every record the authenticated account owns.
      operationId: exportAccount
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserExport"
          description: OK
          headers:
            Content-Disposition:
              schema:
                type: string
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Export your data (GDPR right-of-access)
      tags:
        - account
  /v1/account/suppressions:
    get:
      description: Addresses e2a will refuse to send to (auto-added on a hard bounce or complaint, or added manually). Sends to a suppressed address fail with recipient_suppressed.
      operationId: listSuppressions
      parameters:
        - description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
            type: string
        - description: Maximum number of items to return (1-100).
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            description: Maximum number of items to return (1-100).
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageSuppression"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List suppressed recipient addresses
      tags:
        - account
  /v1/account/suppressions/{address}:
    delete:
      description: Un-suppress a recipient. A previously-blocked send to it then succeeds (idempotency keys are released, so no fresh key is needed). Requires ?confirm=DELETE.
      operationId: deleteSuppression
      parameters:
        - in: path
          name: address
          required: true
          schema:
            type: string
        - description: Must be the literal DELETE — this action is irreversible.
          explode: false
          in: query
          name: confirm
          required: true
          schema:
            description: Must be the literal DELETE — this action is irreversible.
            enum:
              - DELETE
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Remove an address from the suppression list
      tags:
        - account
  /v1/agents:
    get:
      description: List the agents owned by the authenticated account, newest first, with cursor pagination. Pass deleted=true for the trash (soft-deleted agents, restorable until purged).
      operationId: listAgents
      parameters:
        - description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
            type: string
        - description: Maximum number of items to return (1-100).
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            description: Maximum number of items to return (1-100).
            format: int64
            maximum: 100
            minimum: 1
            type: integer
        - description: "List the trash instead: agents that were soft-deleted and are restorable until purged (~30 days). Defaults to false (live agents only)."
          explode: false
          in: query
          name: deleted
          schema:
            description: "List the trash instead: agents that were soft-deleted and are restorable until purged (~30 days). Defaults to false (live agents only)."
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageAgentView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List agents
      tags:
        - agents
    post:
      description: Register an agent by full email. A custom-domain agent's domain must be a verified domain the caller owns; an email on the deployment's shared domain (e.g. xyz@agents.e2a.dev) is registered as a shared-domain agent. Returns the full agent.
      operationId: createAgent
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAgentRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentView"
          description: Created
        "402":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LimitExceededEnvelope"
          description: Payment required — a per-account resource cap was hit (code limit_exceeded). error.details.resource is the AccountView usage/limits field stem (agents, domains, messages_month, storage_bytes), so the client can key it to usage.<resource> / limits.max_<resource>. This is a QUOTA (stock/flow) cap — distinct from a 429 rate_limited (throughput). A retry alone will not clear it; surface a quota/upgrade path.
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RateLimitedEnvelope"
          description: "Too Many Requests — a request-RATE / throughput limit was hit (code rate_limited). This is distinct from a 402 limit_exceeded (a QUOTA cap): a 429 is transient and retry-able — wait error.details.retry_after_seconds (mirrored on the Retry-After header), then the same request succeeds. Branch on the HTTP status: 429 → back off and retry; 402 → surface a quota/upgrade path."
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Create an agent
      tags:
        - agents
  /v1/agents/{email}:
    delete:
      description: Move an agent the caller owns to the trash. Requires ?confirm=DELETE. A trashed agent stops receiving mail, disappears from lists, and its held messages leave the review queue; restore it via POST /v1/agents/{email}/restore within 30 days, after which it is purged permanently (messages included). Pass permanent=true to skip the trash and delete irreversibly right away (accepts live and trashed agents).
      operationId: deleteAgent
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
        - description: Must be the literal DELETE — this action is irreversible.
          explode: false
          in: query
          name: confirm
          required: true
          schema:
            description: Must be the literal DELETE — this action is irreversible.
            enum:
              - DELETE
            type: string
        - description: Delete irreversibly right away instead of moving to the trash. Accepts live and trashed agents.
          explode: false
          in: query
          name: permanent
          schema:
            description: Delete irreversibly right away instead of moving to the trash. Accepts live and trashed agents.
            type: boolean
      responses:
        "204":
          description: No Content
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Delete an agent
      tags:
        - agents
    get:
      description: Fetch a single agent the authenticated account owns, by full email address.
      operationId: getAgent
      parameters:
        - description: The agent's full email address, e.g. support@acme.com.
          in: path
          name: email
          required: true
          schema:
            description: The agent's full email address, e.g. support@acme.com.
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Get an agent
      tags:
        - agents
    patch:
      description: Update an agent's display name. The screening/protection config lives on the /v1/agents/{email}/protection sub-resource. Returns the post-update agent.
      operationId: updateAgent
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAgentRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Update an agent
      tags:
        - agents
  /v1/agents/{email}/conversations:
    get:
      description: List an agent's conversation threads (derived from messages.conversation_id).
      operationId: listConversations
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
        - description: RFC3339.
          explode: false
          in: query
          name: since
          schema:
            description: RFC3339.
            type: string
        - description: RFC3339.
          explode: false
          in: query
          name: until
          schema:
            description: RFC3339.
            type: string
        - description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change since/until.
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change since/until.
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageConversationSummaryView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List conversations
      tags:
        - conversations
  /v1/agents/{email}/conversations/{id}:
    get:
      description: Fetch a single conversation thread with its participants, labels, and member messages.
      operationId: getConversation
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationDetailView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Get a conversation
      tags:
        - conversations
  /v1/agents/{email}/messages:
    get:
      description: List an agent's messages (inbound + outbound) with filters and cursor pagination. Held outbound drafts appear as status=pending_review. Pass deleted=true for the trash (soft-deleted messages, restorable until purged ~30 days after deletion); the trash view defaults to direction=all and read_status=all.
      operationId: listMessages
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
        - description: Defaults to inbound.
          explode: false
          in: query
          name: direction
          schema:
            description: Defaults to inbound.
            enum:
              - inbound
              - outbound
              - all
            type: string
        - description: Inbound only. Filters by inbox read-state (MSG-1). Defaults to unread for inbound, all otherwise.
          explode: false
          in: query
          name: read_status
          schema:
            description: Inbound only. Filters by inbox read-state (MSG-1). Defaults to unread for inbound, all otherwise.
            enum:
              - unread
              - read
              - all
            type: string
        - description: Defaults to desc (newest first).
          explode: false
          in: query
          name: sort
          schema:
            description: Defaults to desc (newest first).
            enum:
              - asc
              - desc
            type: string
        - description: Case-insensitive substring match on sender.
          explode: false
          in: query
          name: from
          schema:
            description: Case-insensitive substring match on sender.
            type: string
        - description: Case-insensitive substring match on subject.
          explode: false
          in: query
          name: subject_contains
          schema:
            description: Case-insensitive substring match on subject.
            type: string
        - explode: false
          in: query
          name: conversation_id
          schema:
            type: string
        - description: Repeatable; AND-matched.
          explode: false
          in: query
          name: labels
          schema:
            description: Repeatable; AND-matched.
            items:
              type: string
            type:
              - array
              - "null"
        - description: RFC3339; created_at >= since.
          explode: false
          in: query
          name: since
          schema:
            description: RFC3339; created_at >= since.
            type: string
        - description: RFC3339; created_at < until.
          explode: false
          in: query
          name: until
          schema:
            description: RFC3339; created_at < until.
            type: string
        - explode: false
          in: query
          name: cursor
          schema:
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int64
            maximum: 100
            minimum: 1
            type: integer
        - description: "List the trash instead: messages that were soft-deleted and are restorable until purged (~30 days after deletion). Defaults to false (live messages only)."
          explode: false
          in: query
          name: deleted
          schema:
            description: "List the trash instead: messages that were soft-deleted and are restorable until purged (~30 days after deletion). Defaults to false (live messages only)."
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageMessageSummaryView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List messages
      tags:
        - messages
    post:
      description: "Send a new email from the agent named in the path (a new thread). The sender is the path agent — `reply`/`forward` are their own sub-resources. 202 + pending_review when the agent has HITL enabled. Honors Idempotency-Key. Attachment limits: at most 10 attachments, each ≤ 10 MB decoded, ≤ 25 MB decoded combined (over-count → 400 invalid_request; over-size → 413 payload_too_large). Two capacity limits apply and are permanently distinct — branch on the HTTP status: 402 limit_exceeded is a QUOTA (monthly-message / storage stock-or-flow cap; a retry will not clear it — surface an upgrade path), 429 rate_limited is a throughput/request-RATE cap (transient; back off Retry-After seconds and retry)."
      operationId: sendMessage
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
        - description: "Optional idempotency key for safe retries (unique per logical request). A retry with the same key and byte-identical body replays the first request's response instead of re-executing it. Completed keys are remembered for at least 24 hours (the published minimum dedup window). Within the window: same key + different body → 422 idempotency_key_reuse (do not retry as-is); same key while the first request is still executing → 409 idempotency_in_flight (wait, then retry unchanged). Dedup is best-effort: under idempotency-store degradation or a mid-request crash the guarantee degrades to at-least-once — a keyed retry may re-execute rather than replay."
          in: header
          name: Idempotency-Key
          schema:
            description: "Optional idempotency key for safe retries (unique per logical request). A retry with the same key and byte-identical body replays the first request's response instead of re-executing it. Completed keys are remembered for at least 24 hours (the published minimum dedup window). Within the window: same key + different body → 422 idempotency_key_reuse (do not retry as-is); same key while the first request is still executing → 409 idempotency_in_flight (wait, then retry unchanged). Dedup is best-effort: under idempotency-store degradation or a mid-request crash the guarantee degrades to at-least-once — a keyed retry may re-execute rather than replay."
            type: string
        - description: "Sync-compat valve. wait=sent holds the request until the message reaches a terminal-or-held state or a bounded timeout (≤20s), then returns that state; on timeout returns status=accepted. Default: no wait. Always branch on body.status, not the HTTP code. No-op until the async pipeline ships — a synchronous server already has the outcome."
          explode: false
          in: query
          name: wait
          schema:
            description: "Sync-compat valve. wait=sent holds the request until the message reaches a terminal-or-held state or a bounded timeout (≤20s), then returns that state; on timeout returns status=accepted. Default: no wait. Always branch on body.status, not the HTTP code. No-op until the async pipeline ships — a synchronous server already has the outcome."
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendEmailRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendResultView"
          description: OK
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendResultView"
          description: "Accepted — durably accepted but not yet delivered: status=accepted (queued for async submission; terminal outcome via GET/webhook events) or status=pending_review (held for human approval)."
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Bad Request — request-shape/validation failure. error.code includes invalid_request (e.g. more than 10 attachments), too_many_recipients, invalid_recipient, invalid_attachment (undecodable base64).
        "402":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LimitExceededEnvelope"
          description: Payment required — a per-account resource cap was hit (code limit_exceeded). error.details.resource is the AccountView usage/limits field stem (agents, domains, messages_month, storage_bytes), so the client can key it to usage.<resource> / limits.max_<resource>. This is a QUOTA (stock/flow) cap — distinct from a 429 rate_limited (throughput). A retry alone will not clear it; surface a quota/upgrade path.
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Conflict — code idempotency_in_flight: a request with this Idempotency-Key is still executing. Retry-able: wait for the first request to finish, then retry with the SAME key and byte-identical body — the retry replays the first request's response instead of re-executing the side effect."
        "413":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Payload Too Large — an attachment exceeds 10 MB decoded, or the combined attachments exceed 25 MB decoded. error.code = payload_too_large; error.details carries the offending size and the limit.
        "422":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Unprocessable — branch on error.code. idempotency_key_reuse: this Idempotency-Key was already used with a DIFFERENT request body (the dedup hash covers the route + the raw body bytes) — do NOT retry as-is; a legitimate retry must resend the byte-identical body, and a genuinely new request needs a fresh key. invalid_request: a semantic validation failure in the request body."
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RateLimitedEnvelope"
          description: "Too Many Requests — a request-RATE / throughput limit was hit (code rate_limited). This is distinct from a 402 limit_exceeded (a QUOTA cap): a 429 is transient and retry-able — wait error.details.retry_after_seconds (mirrored on the Retry-After header), then the same request succeeds. Branch on the HTTP status: 429 → back off and retry; 402 → surface a quota/upgrade path."
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Send a new email
      tags:
        - messages
  /v1/agents/{email}/messages/{id}:
    delete:
      description: Move a message to the trash. Trashed messages disappear from lists, threads, and reply targets, but can be restored via POST …/messages/{id}/restore until they are purged ~30 days after deletion. No confirmation is required because the default delete is reversible. Pass permanent=true with confirm=DELETE to permanently delete a message that is ALREADY in the trash ("delete forever"). A message held for review (review_status=pending_review) cannot be deleted — resolve it in the review queue first (409 message_held).
      operationId: deleteMessage
      parameters:
        - description: The agent's full email address.
          in: path
          name: email
          required: true
          schema:
            description: The agent's full email address.
            type: string
        - description: The message id, e.g. msg_abc123.
          in: path
          name: id
          required: true
          schema:
            description: The message id, e.g. msg_abc123.
            type: string
        - description: Permanently delete a message that is already in the trash (irreversible). Requires confirm=DELETE and an account-scoped credential.
          explode: false
          in: query
          name: permanent
          schema:
            description: Permanently delete a message that is already in the trash (irreversible). Requires confirm=DELETE and an account-scoped credential.
            type: boolean
        - description: Must be the literal DELETE when permanent=true.
          explode: false
          in: query
          name: confirm
          schema:
            description: Must be the literal DELETE when permanent=true.
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Delete a message (move to trash)
      tags:
        - messages
      x-stability: experimental
    get:
      description: Fetch a single message (inbound or outbound) by id, scoped to an agent the caller owns. Includes the raw message and inbound auth headers.
      operationId: getMessage
      parameters:
        - description: The agent's full email address.
          in: path
          name: email
          required: true
          schema:
            description: The agent's full email address.
            type: string
        - description: The message id, e.g. msg_abc123.
          in: path
          name: id
          required: true
          schema:
            description: The message id, e.g. msg_abc123.
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Get a message
      tags:
        - messages
    patch:
      description: Apply a labels delta (`add_labels` / `remove_labels`) to a message the caller owns; returns the post-update label set. Each list is capped at 50 entries; labels are lowercase `[a-z0-9:_-]+` up to 64 chars; the `e2a:` prefix is reserved for system labels. A message carries at most 100 labels. An empty delta is a read of the current labels.
      operationId: updateMessage
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMessageRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateMessageResultView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Update a message (labels)
      tags:
        - messages
  /v1/agents/{email}/messages/{id}/attachments/{index}:
    get:
      description: Returns one attachment's metadata plus a short-lived `download_url` (+ `expires_at`) to fetch the bytes out of band — so binary content never streams through an agent's context. Pass `?inline=true` to also receive base64 `data` for small attachments (<= 256 KB); larger inline requests are rejected with 413 attachment_too_large. `index` is the 0-based attachment index from the message's `attachments[]`.
      operationId: getAttachment
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
        - in: path
          name: id
          required: true
          schema:
            type: string
        - in: path
          name: index
          required: true
          schema:
            format: int64
            minimum: 0
            type: integer
        - description: When true, also include the bytes as base64 in 'data' — ONLY for attachments <= 256 KB; larger inline requests are rejected (413). Default false (use download_url).
          explode: false
          in: query
          name: inline
          schema:
            description: When true, also include the bytes as base64 in 'data' — ONLY for attachments <= 256 KB; larger inline requests are rejected (413). Default false (use download_url).
            type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AttachmentView"
          description: OK
        "413":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Payload Too Large — code attachment_too_large: `?inline=true` was requested for an attachment over the 256 KB inline cap. Fetch the bytes via download_url instead; the metadata (and this error) tells you the size."
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Get an attachment (metadata + short-lived download URL)
      tags:
        - messages
  /v1/agents/{email}/messages/{id}/forward:
    post:
      description: "Forward a message (inbound or outbound) to new recipients; the original is quoted and its attachments are carried over by default. Any attachments[] you supply are added on top of the originals. 202 when held for HITL. Attachment limits apply to the combined set (carried-over originals + supplied): at most 10 attachments, each ≤ 10 MB decoded, ≤ 25 MB decoded combined (over-count → 400 invalid_request; over-size → 413 payload_too_large)."
      operationId: forwardMessage
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
        - in: path
          name: id
          required: true
          schema:
            type: string
        - description: "Optional idempotency key for safe retries (unique per logical request). A retry with the same key and byte-identical body replays the first request's response instead of re-executing it. Completed keys are remembered for at least 24 hours (the published minimum dedup window). Within the window: same key + different body → 422 idempotency_key_reuse (do not retry as-is); same key while the first request is still executing → 409 idempotency_in_flight (wait, then retry unchanged). Dedup is best-effort: under idempotency-store degradation or a mid-request crash the guarantee degrades to at-least-once — a keyed retry may re-execute rather than replay."
          in: header
          name: Idempotency-Key
          schema:
            description: "Optional idempotency key for safe retries (unique per logical request). A retry with the same key and byte-identical body replays the first request's response instead of re-executing it. Completed keys are remembered for at least 24 hours (the published minimum dedup window). Within the window: same key + different body → 422 idempotency_key_reuse (do not retry as-is); same key while the first request is still executing → 409 idempotency_in_flight (wait, then retry unchanged). Dedup is best-effort: under idempotency-store degradation or a mid-request crash the guarantee degrades to at-least-once — a keyed retry may re-execute rather than replay."
            type: string
        - description: "Sync-compat valve. wait=sent holds the request until the message reaches a terminal-or-held state or a bounded timeout (≤20s), then returns that state; on timeout returns status=accepted. Default: no wait. Always branch on body.status, not the HTTP code. No-op until the async pipeline ships — a synchronous server already has the outcome."
          explode: false
          in: query
          name: wait
          schema:
            description: "Sync-compat valve. wait=sent holds the request until the message reaches a terminal-or-held state or a bounded timeout (≤20s), then returns that state; on timeout returns status=accepted. Default: no wait. Always branch on body.status, not the HTTP code. No-op until the async pipeline ships — a synchronous server already has the outcome."
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForwardRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendResultView"
          description: OK
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendResultView"
          description: "Accepted — durably accepted but not yet delivered: status=accepted (queued for async submission; terminal outcome via GET/webhook events) or status=pending_review (held for human approval)."
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Bad Request — request-shape/validation failure. error.code includes invalid_request (e.g. more than 10 attachments), too_many_recipients, invalid_recipient, invalid_attachment (undecodable base64).
        "402":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LimitExceededEnvelope"
          description: Payment required — a per-account resource cap was hit (code limit_exceeded). error.details.resource is the AccountView usage/limits field stem (agents, domains, messages_month, storage_bytes), so the client can key it to usage.<resource> / limits.max_<resource>. This is a QUOTA (stock/flow) cap — distinct from a 429 rate_limited (throughput). A retry alone will not clear it; surface a quota/upgrade path.
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Conflict — code idempotency_in_flight: a request with this Idempotency-Key is still executing. Retry-able: wait for the first request to finish, then retry with the SAME key and byte-identical body — the retry replays the first request's response instead of re-executing the side effect."
        "413":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Payload Too Large — an attachment exceeds 10 MB decoded, or the combined attachments exceed 25 MB decoded. error.code = payload_too_large; error.details carries the offending size and the limit.
        "422":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Unprocessable — branch on error.code. idempotency_key_reuse: this Idempotency-Key was already used with a DIFFERENT request body (the dedup hash covers the route + the raw body bytes) — do NOT retry as-is; a legitimate retry must resend the byte-identical body, and a genuinely new request needs a fresh key. invalid_request: a semantic validation failure in the request body."
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RateLimitedEnvelope"
          description: "Too Many Requests — a request-RATE / throughput limit was hit (code rate_limited). This is distinct from a 402 limit_exceeded (a QUOTA cap): a 429 is transient and retry-able — wait error.details.retry_after_seconds (mirrored on the Retry-After header), then the same request succeeds. Branch on the HTTP status: 429 → back off and retry; 402 → surface a quota/upgrade path."
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Forward a message
      tags:
        - messages
  /v1/agents/{email}/messages/{id}/reply:
    post:
      description: "Reply to a message (inbound or outbound); recipients and threading are derived from the original. Replying to a message the agent received targets its sender; replying to a message the agent sent continues the thread to its original recipients (`reply_all` also re-includes the original Cc). 202 when held for HITL. Attachment limits: at most 10 attachments, each ≤ 10 MB decoded, ≤ 25 MB decoded combined (over-count → 400 invalid_request; over-size → 413 payload_too_large)."
      operationId: replyToMessage
      parameters:
        - in: path
          name: email
          required: true
          schema:
            type: string
        - in: path
          name: id
          required: true
          schema:
            type: string
        - description: "Optional idempotency key for safe retries (unique per logical request). A retry with the same key and byte-identical body replays the first request's response instead of re-executing it. Completed keys are remembered for at least 24 hours (the published minimum dedup window). Within the window: same key + different body → 422 idempotency_key_reuse (do not retry as-is); same key while the first request is still executing → 409 idempotency_in_flight (wait, then retry unchanged). Dedup is best-effort: under idempotency-store degradation or a mid-request crash the guarantee degrades to at-least-once — a keyed retry may re-execute rather than replay."
          in: header
          name: Idempotency-Key
          schema:
            description: "Optional idempotency key for safe retries (unique per logical request). A retry with the same key and byte-identical body replays the first request's response instead of re-executing it. Completed keys are remembered for at least 24 hours (the published minimum dedup window). Within the window: same key + different body → 422 idempotency_key_reuse (do not retry as-is); same key while the first request is still executing → 409 idempotency_in_flight (wait, then retry unchanged). Dedup is best-effort: under idempotency-store degradation or a mid-request crash the guarantee degrades to at-least-once — a keyed retry may re-execute rather than replay."
            type: string
        - description: "Sync-compat valve. wait=sent holds the request until the message reaches a terminal-or-held state or a bounded timeout (≤20s), then returns that state; on timeout returns status=accepted. Default: no wait. Always branch on body.status, not the HTTP code. No-op until the async pipeline ships — a synchronous server already has the outcome."
          explode: false
          in: query
          name: wait
          schema:
            description: "Sync-compat valve. wait=sent holds the request until the message reaches a terminal-or-held state or a bounded timeout (≤20s), then returns that state; on timeout returns status=accepted. Default: no wait. Always branch on body.status, not the HTTP code. No-op until the async pipeline ships — a synchronous server already has the outcome."
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplyRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendResultView"
          description: OK
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendResultView"
          description: "Accepted — durably accepted but not yet delivered: status=accepted (queued for async submission; terminal outcome via GET/webhook events) or status=pending_review (held for human approval)."
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Bad Request — request-shape/validation failure. error.code includes invalid_request (e.g. more than 10 attachments), too_many_recipients, invalid_recipient, invalid_attachment (undecodable base64).
        "402":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LimitExceededEnvelope"
          description: Payment required — a per-account resource cap was hit (code limit_exceeded). error.details.resource is the AccountView usage/limits field stem (agents, domains, messages_month, storage_bytes), so the client can key it to usage.<resource> / limits.max_<resource>. This is a QUOTA (stock/flow) cap — distinct from a 429 rate_limited (throughput). A retry alone will not clear it; surface a quota/upgrade path.
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Conflict — code idempotency_in_flight: a request with this Idempotency-Key is still executing. Retry-able: wait for the first request to finish, then retry with the SAME key and byte-identical body — the retry replays the first request's response instead of re-executing the side effect."
        "413":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Payload Too Large — an attachment exceeds 10 MB decoded, or the combined attachments exceed 25 MB decoded. error.code = payload_too_large; error.details carries the offending size and the limit.
        "422":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Unprocessable — branch on error.code. idempotency_key_reuse: this Idempotency-Key was already used with a DIFFERENT request body (the dedup hash covers the route + the raw body bytes) — do NOT retry as-is; a legitimate retry must resend the byte-identical body, and a genuinely new request needs a fresh key. invalid_request: a semantic validation failure in the request body."
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RateLimitedEnvelope"
          description: "Too Many Requests — a request-RATE / throughput limit was hit (code rate_limited). This is distinct from a 402 limit_exceeded (a QUOTA cap): a 429 is transient and retry-able — wait error.details.retry_after_seconds (mirrored on the Retry-After header), then the same request succeeds. Branch on the HTTP status: 429 → back off and retry; 402 → surface a quota/upgrade path."
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Reply to a message
      tags:
        - messages
  /v1/agents/{email}/messages/{id}/restore:
    post:
      description: Bring a trashed (soft-deleted) message back to the inbox. Its remaining retention resumes where it left off — time spent in the trash does not count against the message's normal lifetime. Returns the restored message. 409 not_in_trash when the message is not in the trash.
      operationId: restoreMessage
      parameters:
        - description: The agent's full email address.
          in: path
          name: email
          required: true
          schema:
            description: The agent's full email address.
            type: string
        - description: The message id, e.g. msg_abc123.
          in: path
          name: id
          required: true
          schema:
            description: The message id, e.g. msg_abc123.
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Restore a message from the trash
      tags:
        - messages
      x-stability: experimental
  /v1/agents/{email}/protection:
    get:
      description: "Read the agent's protection posture — inbound/outbound trust gate, content-scan sensitivity, and hold-queue mechanism. Account scope only: an agent-scoped credential cannot read its own protection config. Beta: the agent protection config is unstable — its shape may change before it is declared stable."
      operationId: getAgentProtection
      parameters:
        - description: The agent's full email address.
          in: path
          name: email
          required: true
          schema:
            description: The agent's full email address.
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProtectionConfigView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Get an agent's protection config (beta)
      tags:
        - agents
      x-stability: experimental
    put:
      description: "Replace the agent's protection posture wholesale. The three top-level keys (inbound, outbound, holds) are required; leaves default. Account scope only. Beta: the agent protection config is unstable — its shape may change before it is declared stable."
      operationId: putAgentProtection
      parameters:
        - description: The agent's full email address.
          in: path
          name: email
          required: true
          schema:
            description: The agent's full email address.
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProtectionConfigRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProtectionConfigView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Replace an agent's protection config (beta)
      tags:
        - agents
      x-stability: experimental
  /v1/agents/{email}/restore:
    post:
      description: Bring a trashed (soft-deleted) agent back into service, messages and configuration intact. Returns the restored agent. 409 not_in_trash when the agent is not in the trash.
      operationId: restoreAgent
      parameters:
        - description: The agent's full email address, e.g. support@acme.com.
          in: path
          name: email
          required: true
          schema:
            description: The agent's full email address, e.g. support@acme.com.
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Restore an agent from the trash
      tags:
        - agents
      x-stability: experimental
  /v1/agents/{email}/test:
    post:
      description: Send a platform test email to the agent's own address to confirm inbound delivery. 202 when held for HITL.
      operationId: testAgent
      parameters:
        - description: The agent's full email address, e.g. support@acme.com.
          in: path
          name: email
          required: true
          schema:
            description: The agent's full email address, e.g. support@acme.com.
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendResultView"
          description: OK
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendResultView"
          description: "Accepted — durably accepted but not yet delivered: status=accepted (queued for async submission; terminal outcome via GET/webhook events) or status=pending_review (held for human approval)."
        "402":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LimitExceededEnvelope"
          description: Payment required — a per-account resource cap was hit (code limit_exceeded). error.details.resource is the AccountView usage/limits field stem (agents, domains, messages_month, storage_bytes), so the client can key it to usage.<resource> / limits.max_<resource>. This is a QUOTA (stock/flow) cap — distinct from a 429 rate_limited (throughput). A retry alone will not clear it; surface a quota/upgrade path.
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RateLimitedEnvelope"
          description: "Too Many Requests — a request-RATE / throughput limit was hit (code rate_limited). This is distinct from a 402 limit_exceeded (a QUOTA cap): a 429 is transient and retry-able — wait error.details.retry_after_seconds (mirrored on the Retry-After header), then the same request succeeds. Branch on the HTTP status: 429 → back off and retry; 402 → surface a quota/upgrade path."
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Send a test email to the agent's own address
      tags:
        - agents
  /v1/domains:
    get:
      description: List the domains owned by the authenticated account, newest first, with cursor pagination.
      operationId: listDomains
      parameters:
        - description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
            type: string
        - description: Maximum number of items to return (1-100).
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            description: Maximum number of items to return (1-100).
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageDomainView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List domains
      tags:
        - domains
    post:
      operationId: registerDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterDomainRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainView"
          description: Created
        "402":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LimitExceededEnvelope"
          description: Payment required — a per-account resource cap was hit (code limit_exceeded). error.details.resource is the AccountView usage/limits field stem (agents, domains, messages_month, storage_bytes), so the client can key it to usage.<resource> / limits.max_<resource>. This is a QUOTA (stock/flow) cap — distinct from a 429 rate_limited (throughput). A retry alone will not clear it; surface a quota/upgrade path.
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Conflict — the domain is already claimed by another account (code domain_taken).
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Register a domain
      tags:
        - domains
  /v1/domains/{domain}:
    delete:
      description: Deprovisions the domain's sending identity and breaks sending for every agent on it. Requires ?confirm=DELETE (irreversible).
      operationId: deleteDomain
      parameters:
        - in: path
          name: domain
          required: true
          schema:
            type: string
        - description: Must be the literal DELETE — this action is irreversible.
          explode: false
          in: query
          name: confirm
          required: true
          schema:
            description: Must be the literal DELETE — this action is irreversible.
            enum:
              - DELETE
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Delete a domain
      tags:
        - domains
    get:
      operationId: getDomain
      parameters:
        - in: path
          name: domain
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Get a domain
      tags:
        - domains
  /v1/domains/{domain}/verify:
    post:
      description: Probe the domain's published DNS and, when the verification TXT (and inbound MX) are present, mark it verified. Always returns 200 with the per-record diagnostic — branch on the `verified` boolean in the body, not the HTTP status. A not-yet-published record is the normal `verified:false` outcome, not an error.
      operationId: verifyDomain
      parameters:
        - in: path
          name: domain
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerifyDomainView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Verify a domain
      tags:
        - domains
  /v1/events:
    get:
      description: The webhook-event delivery log, filterable by type/agent/conversation/message and time range, with cursor pagination.
      operationId: listEvents
      parameters:
        - explode: false
          in: query
          name: type
          schema:
            type: string
        - explode: false
          in: query
          name: agent_email
          schema:
            type: string
        - explode: false
          in: query
          name: conversation_id
          schema:
            type: string
        - explode: false
          in: query
          name: message_id
          schema:
            type: string
        - description: RFC3339.
          explode: false
          in: query
          name: since
          schema:
            description: RFC3339.
            type: string
        - description: RFC3339.
          explode: false
          in: query
          name: until
          schema:
            description: RFC3339.
            type: string
        - explode: false
          in: query
          name: cursor
          schema:
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageEventJSON"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List events
      tags:
        - events
  /v1/events/{id}:
    get:
      operationId: getEvent
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventJSON"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Get an event
      tags:
        - events
  /v1/events/{id}/redeliver:
    post:
      description: "Re-enqueue webhook delivery for an event. With a webhook_id, replays to that subscriber; without, fans out to every originally-matched subscriber. Auto-deduplicated within a short window — receivers must dedup on event id. Returns 202 Accepted: the redelivery is durably enqueued for async submission, not delivered synchronously — the per-subscriber outcome surfaces via the delivery log, and each delivery's status is 'pending' (or 'scheduled' for the fan-out)."
      operationId: redeliverEvent
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RedeliverEventRequest"
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RedeliverView"
          description: Accepted
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Redeliver an event
      tags:
        - events
  /v1/info:
    get:
      description: "Public deployment metadata: the shared agent domain (if slug registration is enabled) and the public base URL. Unauthenticated."
      operationId: getInfo
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeploymentInfoView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      summary: Deployment info
      tags:
        - meta
  /v1/reviews:
    get:
      description: "The review queue: every message held in pending_review across the account's inboxes — outbound drafts awaiting send approval AND inbound messages held by a screening gate. Account-scoped credentials only; agents cannot see (or resolve) holds."
      operationId: listReviews
      parameters:
        - description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
            type: string
        - description: Maximum number of items to return (1-100).
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            description: Maximum number of items to return (1-100).
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageReviewView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List messages awaiting review
      tags:
        - reviews
  /v1/reviews/{id}:
    get:
      description: Full detail of one held message — body + recipients (and, for inbound, the screening/auth context) — for a reviewer to make a decision. Account-scoped only.
      operationId: getReview
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Get a held message (full detail)
      tags:
        - reviews
  /v1/reviews/{id}/approve:
    post:
      description: "Approve a hold. Branches on direction: an outbound draft is sent via SES (honoring Idempotency-Key + optional reviewer overrides); an inbound hold is released to the inbox. Account-scoped only — an agent cannot approve its own hold. Approving an outbound draft applies the same per-agent send-rate limit as a direct send: 429 rate_limited when the agent is over its throughput limit (back off Retry-After seconds and retry)."
      operationId: approveReview
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
        - description: "Optional idempotency key for safe retries (unique per logical request). A retry with the same key and byte-identical body replays the first request's response instead of re-executing it. Completed keys are remembered for at least 24 hours (the published minimum dedup window). Within the window: same key + different body → 422 idempotency_key_reuse (do not retry as-is); same key while the first request is still executing → 409 idempotency_in_flight (wait, then retry unchanged). Dedup is best-effort: under idempotency-store degradation or a mid-request crash the guarantee degrades to at-least-once — a keyed retry may re-execute rather than replay."
          in: header
          name: Idempotency-Key
          schema:
            description: "Optional idempotency key for safe retries (unique per logical request). A retry with the same key and byte-identical body replays the first request's response instead of re-executing it. Completed keys are remembered for at least 24 hours (the published minimum dedup window). Within the window: same key + different body → 422 idempotency_key_reuse (do not retry as-is); same key while the first request is still executing → 409 idempotency_in_flight (wait, then retry unchanged). Dedup is best-effort: under idempotency-store degradation or a mid-request crash the guarantee degrades to at-least-once — a keyed retry may re-execute rather than replay."
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendResultView"
          description: OK
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Conflict — branch on error.code. message_not_pending: the hold was already resolved (approved/rejected/expired) and cannot be re-approved. idempotency_in_flight: a request with this Idempotency-Key is still executing — wait for it to finish, then retry with the SAME key and byte-identical body to replay its response."
        "422":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Unprocessable — branch on error.code. idempotency_key_reuse: this Idempotency-Key was already used with a DIFFERENT request body (the dedup hash covers the route + the raw body bytes) — do NOT retry as-is; a legitimate retry must resend the byte-identical body, and a genuinely new request needs a fresh key. invalid_request: a semantic validation failure in the request body."
        "429":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RateLimitedEnvelope"
          description: "Too Many Requests — a request-RATE / throughput limit was hit (code rate_limited). This is distinct from a 402 limit_exceeded (a QUOTA cap): a 429 is transient and retry-able — wait error.details.retry_after_seconds (mirrored on the Retry-After header), then the same request succeeds. Branch on the HTTP status: 429 → back off and retry; 402 → surface a quota/upgrade path."
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Approve a held message
      tags:
        - reviews
  /v1/reviews/{id}/reject:
    post:
      description: Reject a hold. An outbound draft is discarded (never sent); an inbound hold is dropped (never reaches the agent; payload retained hidden for forensics). Account-scoped only.
      operationId: rejectReview
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RejectResultView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Reject a held message
      tags:
        - reviews
  /v1/starter-templates:
    get:
      description: "List the pre-built starter templates shipped with the deployment, sorted by alias. Returns catalog metadata only; fetch one by alias for the full body sources, or copy one into your library with from_starter on POST /v1/templates. Beta: templates are unstable — their shape may change before they are declared stable."
      operationId: listStarterTemplates
      parameters:
        - description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
            type: string
        - description: Maximum number of items to return (1-100).
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            description: Maximum number of items to return (1-100).
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageStarterTemplateView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List starter templates (beta)
      tags:
        - templates
      x-stability: experimental
  /v1/starter-templates/{alias}:
    get:
      description: "Fetch one starter template by alias, including its full plain-text and HTML body sources. Beta: templates are unstable — their shape may change before they are declared stable."
      operationId: getStarterTemplate
      parameters:
        - description: The starter template's alias, e.g. welcome.
          in: path
          name: alias
          required: true
          schema:
            description: The starter template's alias, e.g. welcome.
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StarterTemplateDetailView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Get a starter template (beta)
      tags:
        - templates
      x-stability: experimental
  /v1/templates:
    get:
      description: "List the account's templates, newest first. Returns metadata only (no text/html); fetch one by id for the full sources. Beta: templates are unstable — their shape may change before they are declared stable."
      operationId: listTemplates
      parameters:
        - description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
            type: string
        - description: Maximum number of items to return (1-100).
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            description: Maximum number of items to return (1-100).
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageTemplateSummaryView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List templates (beta)
      tags:
        - templates
      x-stability: experimental
    post:
      description: "Create a reusable email template. subject and text (and html when present) must parse: {{variable}} interpolation with dot paths; {{{variable}}} renders raw in the HTML part. Alternatively set from_starter to copy a starter template verbatim. Beta: templates are unstable — their shape may change before they are declared stable."
      operationId: createTemplate
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTemplateRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TemplateView"
          description: Created
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Create a template (beta)
      tags:
        - templates
      x-stability: experimental
  /v1/templates/validate:
    post:
      description: "Dry-run template source without persisting: reports per-part parse errors, a rendered preview (against test_data when provided), and suggested_data — a placeholder value for every variable the source references. Beta: templates are unstable — their shape may change before they are declared stable."
      operationId: validateTemplate
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ValidateTemplateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidateTemplateResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Validate template source (beta)
      tags:
        - templates
      x-stability: experimental
  /v1/templates/{id}:
    delete:
      description: "Delete a template. In-flight sends are unaffected (rendering happens at send time). Requires ?confirm=DELETE. Beta: templates are unstable — their shape may change before they are declared stable."
      operationId: deleteTemplate
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
        - description: Must be the literal DELETE — this action is irreversible.
          explode: false
          in: query
          name: confirm
          required: true
          schema:
            description: Must be the literal DELETE — this action is irreversible.
            enum:
              - DELETE
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Delete a template (beta)
      tags:
        - templates
      x-stability: experimental
    get:
      description: "Fetch one template by id. Beta: templates are unstable — their shape may change before they are declared stable."
      operationId: getTemplate
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TemplateView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Get a template (beta)
      tags:
        - templates
      x-stability: experimental
    patch:
      description: "Partial update. Changed template parts are re-parsed; set alias or html to \"\" to clear them. Beta: templates are unstable — their shape may change before they are declared stable."
      operationId: updateTemplate
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTemplateRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TemplateView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Update a template (beta)
      tags:
        - templates
      x-stability: experimental
  /v1/webhooks:
    get:
      description: List the webhooks owned by the authenticated account, newest first, with cursor pagination.
      operationId: listWebhooks
      parameters:
        - description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the other filters.
            type: string
        - description: Maximum number of items to return (1-100).
          explode: false
          in: query
          name: limit
          schema:
            default: 100
            description: Maximum number of items to return (1-100).
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageWebhookView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List webhooks
      tags:
        - webhooks
    post:
      operationId: createWebhook
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWebhookRequest"
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateWebhookResponse"
          description: Created
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Create a webhook
      tags:
        - webhooks
  /v1/webhooks/{id}:
    delete:
      description: Delete a webhook subscriber by id. Requires ?confirm=DELETE.
      operationId: deleteWebhook
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
        - description: Must be the literal DELETE — this action is irreversible.
          explode: false
          in: query
          name: confirm
          required: true
          schema:
            description: Must be the literal DELETE — this action is irreversible.
            enum:
              - DELETE
            type: string
      responses:
        "204":
          description: No Content
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Delete a webhook
      tags:
        - webhooks
    get:
      operationId: getWebhook
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Get a webhook
      tags:
        - webhooks
    patch:
      description: Partial update. url/events/filters are full-replace when present. Re-enabling within the auto-disable cooldown returns 409.
      operationId: updateWebhook
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWebhookRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookView"
          description: OK
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Conflict — code webhook_cooldown: the webhook was auto-disabled for persistent delivery failures and cannot be re-enabled until the cooldown elapses. Retry after the cooldown, or fix the endpoint first."
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Update a webhook
      tags:
        - webhooks
  /v1/webhooks/{id}/deliveries:
    get:
      description: The per-webhook delivery log (read-only debug view).
      operationId: listWebhookDeliveries
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
        - explode: false
          in: query
          name: status
          schema:
            enum:
              - pending
              - delivered
              - failed
            type: string
        - description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the status filter.
          explode: false
          in: query
          name: cursor
          schema:
            description: Opaque pagination cursor from a previous response's next_cursor. Continuation requests must not change the status filter.
            type: string
        - explode: false
          in: query
          name: limit
          schema:
            default: 100
            format: int64
            maximum: 100
            minimum: 1
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PageWebhookDeliveryView"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: List webhook deliveries
      tags:
        - webhooks
  /v1/webhooks/{id}/rotate-secret:
    post:
      description: Mint a new signing secret; the previous one stays valid for a 24h grace window. Returns the new secret (shown once). Honors Idempotency-Key so a retried rotate replays the same secret instead of rotating twice (rotate has no request body, so the dedup hash covers the route alone — the same key on a different webhook id is a 422 idempotency_key_reuse).
      operationId: rotateWebhookSecret
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
        - description: "Optional idempotency key for safe retries (unique per logical request). A retry with the same key and byte-identical body replays the first request's response instead of re-executing it. Completed keys are remembered for at least 24 hours (the published minimum dedup window). Within the window: same key + different body → 422 idempotency_key_reuse (do not retry as-is); same key while the first request is still executing → 409 idempotency_in_flight (wait, then retry unchanged). Dedup is best-effort: under idempotency-store degradation or a mid-request crash the guarantee degrades to at-least-once — a keyed retry may re-execute rather than replay."
          in: header
          name: Idempotency-Key
          schema:
            description: "Optional idempotency key for safe retries (unique per logical request). A retry with the same key and byte-identical body replays the first request's response instead of re-executing it. Completed keys are remembered for at least 24 hours (the published minimum dedup window). Within the window: same key + different body → 422 idempotency_key_reuse (do not retry as-is); same key while the first request is still executing → 409 idempotency_in_flight (wait, then retry unchanged). Dedup is best-effort: under idempotency-store degradation or a mid-request crash the guarantee degrades to at-least-once — a keyed retry may re-execute rather than replay."
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RotateSecretResponse"
          description: OK
        "409":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Conflict — code idempotency_in_flight: a request with this Idempotency-Key is still executing. Retry-able: wait for the first request to finish, then retry with the SAME key and byte-identical body — the retry replays the first request's response instead of re-executing the side effect."
        "422":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: "Unprocessable — branch on error.code. idempotency_key_reuse: this Idempotency-Key was already used with a DIFFERENT request body (the dedup hash covers the route + the raw body bytes) — do NOT retry as-is; a legitimate retry must resend the byte-identical body, and a genuinely new request needs a fresh key. invalid_request: a semantic validation failure in the request body."
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error — the standard envelope; branch on error.code.
      security:
        - bearer: []
      summary: Rotate a webhook signing secret
      tags:
        - webhooks
  /v1/webhooks/{id}/test:
    post:
      description: Schedule a one-off synthetic delivery to this webhook for development. Returns the delivery id.
      operationId: testWebhook
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestWebhookRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestWebhookResponse"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
          description: Error
      security:
        - bearer: []
      summary: Fire a synthetic event
      tags:
        - webhooks
servers:
  - description: Production
    url: https://api.e2a.dev
